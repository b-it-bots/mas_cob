<launch>

  <!-- Set the file describing which sensor(s) to use to build the octomap  -->
  <rosparam command="load" file="$(find mas_cob_moveit)/config/sensors_rgbd.yaml" />
  <rosparam command="load" file="$(find mas_cob_moveit)/config/octomap.yaml" />

</launch>
